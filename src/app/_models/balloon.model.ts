import { Position } from './position.model';

export interface Balloons {
  _id: string;
  name: string;
  color: string;
  description: string;
  type: string;
  longitude: string;
  latitude: string;
  altitude: string;
  user_id: string;
}
